package com.company;

public class Snail {
    public void realSnail(int[][] array) {
        int[] toArray = new int[array.length * array.length];

        System.out.println(toArray.length);
        int k = array.length;
        int j = array.length;

    }
}
